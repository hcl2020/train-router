{
  "name": "train-router",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "start nodemon ./server/bin/www && cd client && npm run start"
  },
  "devDependencies": {
    "nodemon": "^1.19.2"
  }
}
